import { StyleSheet } from "react-native";
import colors from "../../utils/colors";

export const styles = StyleSheet.create({
    container:{
        borderBottomWidth:colors.primary,
        borderBottomWidth:1,
        marginVertical:20
    }
})